# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# local database (SQLite dev fallback)
payload.db
payload.db-*
*.db-journal

# locally stored uploads (dev) — production uses Vercel Blob
/media

# seed assets (large videos/images, uploaded to media storage by the seed script)
/seed-assets

# env
.env
.env*.local

# misc
.DS_Store
*.tsbuildinfo

# local screenshots / scratch scripts (never the build configs)
/*.png
/*.mjs
!next.config.mjs
!postcss.config.mjs
